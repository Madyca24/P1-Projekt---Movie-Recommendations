#include <stdio.h>
#include <math.h>

int main(void){
    int u1, u2, v1, v2, w1, w2;
    scanf(" %d %d %d %d", &u1, &u2, &u1, &u2);
    printf("den samlede vektor er: %d %d", add(u1, u2, v1, v2));
    

    

}